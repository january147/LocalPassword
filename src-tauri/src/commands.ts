import type { list_passwords } from './commands';
import type { add_password } from './commands';
import type { update_password } from './commands';
import type { delete_password } from './commands';
import type { generate_password } from './commands';
import type { export_passwords } from './commands';
import type { import_passwords } from './commands';
import type { init_database } from './commands';
import type { cleanup_database } from './commands';

window.addEventListener('DOMContentLoaded', () => {
  console.log('Password Manager Raw - Loaded');
});
