[package]
name = "password_manager_raw"
version = "2.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2.1", features = ["shell-open"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
tauri-build = { version = "2.1", features = [] }
